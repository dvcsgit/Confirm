@model Models.ASE.QS.CheckItemManagement.GridViewModel

<div class="widget-box widget-color-dark">
    <div class="widget-header">
        <h5 class="widget-title bigger lighter">
            @Resources.Resource.SearchResults
        </h5>
        @if (ViewBag.CanCreate)
        {
            <div class="widget-toolbar">
                <a href="#" onclick="Create(); return false;">
                    <i class="ace-icon fa fa-plus white"></i>
                </a>
            </div>
        }
    </div>

    <div class="widget-body">
        <div class="widget-main no-padding">
            <table class="table table-striped table-bordered table-hover">
                <thead class="thin-border-bottom">
                    <tr>
                        <th class="col-sm-1"></th>
                        <th class="col-sm-2 center">稽核類別代號</th>
                        <th class="col-sm-4">稽核類別中文描述</th>
                        <th class="col-sm-5">稽核類別英文描述</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.ItemList)
                    {
                        <tr>
                            <td class="col-sm-1 center">
                                <div class="action-buttons">
                                    <a class="blue" href="#" onclick="Detail('@item.ID');" title="@Resources.Resource.Detail">
                                        <i class="ace-icon fa fa-search-plus bigger-130"></i>
                                    </a>

                                    @if (ViewBag.CanEdit)
                                    {
                                        <a class="green" href="#" onclick="Edit('@item.ID');" title="@Resources.Resource.Edit">
                                            <i class="ace-icon fa fa-pencil bigger-130"></i>
                                        </a>
                                    }

                                    @if (ViewBag.CanDelete)
                                    {
                                        <a class="red" href="#" onclick="Delete('@item.ID');" title="@Resources.Resource.Delete">
                                            <i class="ace-icon fa fa-trash-o bigger-130"></i>
                                        </a>
                                    }
                                </div>
                            </td>
                            <td class="center">@item.ID</td>
                            <td>@item.CDescription</td>
                            <td>@item.EDescription</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
